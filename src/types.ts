export type TypeColumn = {
  id: string;
  title: string;
};

export type TypeTask = {
  _id: string;
  title: string;
  description: string;
  status: string;
  createdAt: string;
  updatedAt: string;
  createdBy: string;
};
